"""Local, deterministic ML engine: course catalog, TF-IDF+SVD semantic space,
prerequisite DAG, multi-factor ranker, and phased path planner.

Public entrypoint: `from app.ml.engine import engine`
"""
