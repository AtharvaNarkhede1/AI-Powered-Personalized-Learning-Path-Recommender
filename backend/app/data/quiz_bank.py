_Q = lambda t, o, i, e: {"question_text": t, "options": o, "correct_option_index": i, "explanation": e}
SKILL_QUIZ_BANK = {
    "advanced python & scientific computing": [
        _Q("What does `list(map(lambda x: x*2, filter(lambda x: x % 2, range(5))))` return?",
           ["[0, 4, 8]", "[2, 6]", "[1, 3]", "[0, 2, 4, 6, 8]"], 1,
           "filter keeps odd numbers 1 and 3; map doubles them to 2 and 6."),
        _Q("In NumPy, what is the main advantage of a vectorised operation over a Python for-loop?",
           ["It uses less memory always", "It runs in compiled C without per-element Python overhead",
            "It is easier to read only", "It avoids floating-point error"], 1,
           "NumPy pushes the loop into optimised C, avoiding interpreter overhead per element."),
        _Q("Which statement about Python's `@classmethod` is correct?",
           ["It cannot access the class", "Its first argument is the class, not the instance",
            "It is the same as `@staticmethod`", "It can only be called on an instance"], 1,
           "A classmethod receives `cls` and can construct or configure the class itself."),
        _Q("A NumPy array slice `a[1:3]` returns a view. What does that imply?",
           ["Modifying the slice also modifies `a`", "The slice is a deep copy",
            "The slice is read-only", "The slice has its own dtype"], 0,
           "Basic slicing returns a view that shares memory with the original array."),
    ],
    "linear algebra, calculus & probability": [
        _Q("If A is 3x2 and B is 2x4, what is the shape of AB?",
           ["3x4", "2x2", "4x3", "undefined"], 0,
           "Inner dimensions (2 and 2) match; the result takes the outer dimensions 3x4."),
        _Q("The gradient of a scalar function f(x, y) points in the direction of…",
           ["steepest descent", "steepest ascent", "zero change", "the x-axis"], 1,
           "The gradient points toward the greatest rate of increase; -gradient is steepest descent."),
        _Q("Two events A and B are independent when:",
           ["P(A and B) = P(A) + P(B)", "P(A and B) = P(A) * P(B)",
            "P(A|B) = 0", "P(A) = P(B)"], 1,
           "Independence means the joint probability factors into the product of the marginals."),
        _Q("A square matrix is singular (non-invertible) when:",
           ["its determinant is zero", "it is symmetric", "it is diagonal", "all entries are positive"], 0,
           "A zero determinant means the columns are linearly dependent, so no inverse exists."),
    ],
    "classical machine learning (scikit-learn)": [
        _Q("High training accuracy but low test accuracy is a sign of:",
           ["underfitting", "overfitting", "data leakage prevention", "good generalisation"], 1,
           "The model memorised the training set and fails to generalise - classic overfitting."),
        _Q("Why must you fit a `StandardScaler` on the training set only?",
           ["To save time", "To avoid leaking test-set statistics into training",
            "Scikit-learn requires it", "To reduce the number of features"], 1,
           "Fitting on all data lets test information influence the model - a leakage bug."),
        _Q("Which metric is most appropriate for a highly imbalanced classification problem?",
           ["plain accuracy", "precision/recall or F1", "mean squared error", "R^2"], 1,
           "Accuracy is misleading when one class dominates; precision/recall/F1 expose it."),
        _Q("k-fold cross-validation is used primarily to:",
           ["speed up training", "get a more reliable estimate of out-of-sample performance",
            "increase the dataset size", "remove outliers"], 1,
           "Averaging over folds reduces the variance of the performance estimate."),
    ],
    "deep learning & pytorch": [
        _Q("In PyTorch, what does `optimizer.zero_grad()` do?",
           ["Deletes the optimizer", "Clears accumulated gradients before the next backward pass",
            "Sets the learning rate to zero", "Freezes the model"], 1,
           "Gradients accumulate by default, so they must be cleared each iteration."),
        _Q("A vanishing-gradient problem is commonly mitigated by:",
           ["using sigmoid everywhere", "ReLU activations and residual/skip connections",
            "removing all normalisation", "training with a huge learning rate"], 1,
           "ReLU and residual connections keep gradient magnitudes usable in deep networks."),
        _Q("What is the role of a validation set during training?",
           ["It trains the weights", "It tunes hyperparameters and detects overfitting early",
            "It is the final report metric", "It is never used"], 1,
           "The validation set guides early stopping and hyperparameter choices without touching test data."),
        _Q("Dropout works by:",
           ["adding noise to labels", "randomly zeroing activations during training to reduce co-adaptation",
            "increasing the learning rate", "removing layers permanently"], 1,
           "Randomly dropping units forces redundant, more robust representations."),
    ],
    "llms, embeddings & rag systems": [
        _Q("In a RAG pipeline, what is retrieved and injected into the prompt?",
           ["model weights", "relevant document chunks found by embedding similarity",
            "the user's password", "training gradients"], 1,
           "RAG retrieves semantically similar chunks from a vector store and adds them as context."),
        _Q("Why use embeddings rather than keyword search for retrieval?",
           ["They are always faster", "They capture semantic similarity, matching meaning not just words",
            "They need no storage", "They are exact-match only"], 1,
           "Embedding similarity finds passages about the same concept even with different wording."),
        _Q("A model 'hallucination' in an LLM answer means:",
           ["the answer is too long", "the model states something fluent but factually unsupported",
            "the model refuses to answer", "the temperature is 0"], 1,
           "Hallucination is confident, plausible text not grounded in the provided context or facts."),
        _Q("Lowering the sampling `temperature` toward 0 makes output:",
           ["more random", "more deterministic and focused on the most likely tokens",
            "longer", "multilingual"], 1,
           "Low temperature sharpens the distribution toward the highest-probability tokens."),
    ],
    "c++ & ros 2 development": [
        _Q("In ROS 2, nodes exchange streaming data through:",
           ["services", "topics with publishers and subscribers", "parameters", "launch files"], 1,
           "Topics carry many-to-many asynchronous message streams; services are request/response."),
        _Q("What does RAII in C++ guarantee?",
           ["Runtime array bounds checks", "Resources are released when the owning object goes out of scope",
            "Automatic multithreading", "Garbage collection"], 1,
           "RAII ties resource lifetime to object lifetime, freeing it in the destructor."),
        _Q("A ROS 2 service call is best suited for:",
           ["high-rate sensor streams", "an occasional request that expects one response",
            "logging", "parameter storage"], 1,
           "Services model synchronous request/response; topics handle continuous streams."),
        _Q("`std::unique_ptr` differs from `std::shared_ptr` because it:",
           ["allows many owners", "has exclusive ownership and zero reference-count overhead",
            "never frees memory", "is only for arrays"], 1,
           "unique_ptr is move-only with a single owner; shared_ptr tracks a reference count."),
    ],
    "embedded c & data structures": [
        _Q("Why is dynamic memory allocation (`malloc`) often avoided in embedded firmware?",
           ["It is undefined in C", "Fragmentation and non-deterministic timing on constrained devices",
            "It is slower to type", "It disables interrupts"], 1,
           "Heap fragmentation and unpredictable latency are unacceptable in many real-time systems."),
        _Q("The `volatile` keyword tells the compiler that a variable:",
           ["is constant", "may change outside normal program flow (e.g. an ISR or hardware register)",
            "should be inlined", "is thread-local"], 1,
           "`volatile` prevents caching the value in a register, forcing a re-read each access."),
        _Q("A circular (ring) buffer is commonly used for:",
           ["sorting", "buffering data between an interrupt producer and a main-loop consumer",
            "floating-point math", "recursion"], 1,
           "Ring buffers give O(1) enqueue/dequeue with fixed memory - ideal for ISR-to-loop queues."),
        _Q("What is the time complexity of searching a sorted array with binary search?",
           ["O(n)", "O(log n)", "O(1)", "O(n log n)"], 1,
           "Binary search halves the search space each step."),
    ],
    "embedded microcontrollers & microprocessors": [
        _Q("An interrupt service routine (ISR) should be:",
           ["as long as possible", "short, doing minimal work and deferring the rest to the main loop",
            "written in Python", "used for all program logic"], 1,
           "Long ISRs block other interrupts and hurt real-time responsiveness."),
        _Q("A GPIO pin configured as input with a pull-up resistor reads what when left floating?",
           ["logic 0", "logic 1", "undefined noise", "high impedance only"], 1,
           "The pull-up holds the line at logic high until something drives it low."),
        _Q("PWM is typically used to:",
           ["send serial data", "control average power to a load (motor speed, LED brightness)",
            "measure temperature", "generate interrupts"], 1,
           "Varying the duty cycle sets the average voltage delivered to the load."),
        _Q("A watchdog timer protects an embedded system by:",
           ["saving power", "resetting the MCU if the firmware fails to 'kick' it in time",
            "encrypting memory", "increasing clock speed"], 1,
           "If the main loop hangs, the watchdog expires and forces a recovery reset."),
    ],
    "real-time operating systems (freertos)": [
        _Q("In an RTOS, a higher-priority task that becomes ready will:",
           ["wait its turn", "preempt a running lower-priority task",
            "be ignored", "lower its own priority"], 1,
           "Preemptive schedulers immediately switch to the highest-priority ready task."),
        _Q("Priority inversion is best mitigated with:",
           ["disabling interrupts forever", "priority inheritance on mutexes",
            "busy-waiting", "removing all mutexes"], 1,
           "Priority inheritance temporarily raises the holder's priority to the blocked task's level."),
        _Q("A binary semaphore is commonly used to:",
           ["store data", "signal an event from an ISR to a task",
            "allocate memory", "measure time"], 1,
           "The ISR 'gives' the semaphore and a task blocked on 'take' wakes up to handle the event."),
        _Q("Deterministic timing means:",
           ["the system is fast", "worst-case response time is bounded and predictable",
            "no interrupts are used", "the CPU never idles"], 1,
           "Real-time correctness is about meeting deadlines every time, not raw speed."),
    ],
    "control systems (pid, state-space)": [
        _Q("Increasing the integral gain (Ki) in a PID controller primarily:",
           ["reduces steady-state error but can add overshoot/oscillation", "adds damping",
            "has no effect", "only speeds up the sensor"], 0,
           "The integral term eliminates steady-state offset but too much Ki destabilises the loop."),
        _Q("The derivative term (Kd) in a PID controller mainly:",
           ["removes steady-state error", "adds damping by reacting to the rate of change of error",
            "increases the setpoint", "filters the reference"], 1,
           "Kd anticipates error trend and reduces overshoot, but amplifies measurement noise."),
        _Q("A system is stable (LTI) if all closed-loop poles lie:",
           ["in the right half-plane", "in the left half-plane (negative real parts)",
            "on the imaginary axis", "at the origin"], 1,
           "Left-half-plane poles give decaying responses; right-half-plane poles diverge."),
        _Q("State-space representation describes a system with:",
           ["only a transfer function", "first-order differential equations in state variables (A, B, C, D)",
            "a single gain", "frequency response only"], 1,
           "The state-space form captures internal states and generalises to MIMO systems."),
    ],
    "linux systems administration & shell scripting": [
        _Q("What do the permission bits `rwxr-x---` (750) on a directory allow?",
           ["everyone can read", "owner full access, group read+enter, others nothing",
            "no one can write", "only root can read"], 1,
           "750 = owner rwx, group r-x, others no access."),
        _Q("In a shell pipeline `a | b`, the stdout of `a` becomes:",
           ["a file", "the stdin of `b`", "an environment variable", "discarded"], 1,
           "The pipe connects a's standard output to b's standard input."),
        _Q("`chmod +x script.sh` does what?",
           ["runs the script", "marks the file executable", "deletes the file", "changes the owner"], 1,
           "It adds the execute permission so the file can be run directly."),
        _Q("Which signal does `kill -9 <pid>` send, and why is it a last resort?",
           ["SIGTERM, graceful", "SIGKILL, cannot be caught so no cleanup runs",
            "SIGHUP, reload config", "SIGSTOP, pause only"], 1,
           "SIGKILL is unblockable and immediate - the process gets no chance to clean up."),
    ],
    "docker containers & kubernetes": [
        _Q("A Docker image differs from a container in that the image is:",
           ["a running process", "a read-only template; the container is a running instance of it",
            "a network", "a volume"], 1,
           "You build an image once and run many containers from it."),
        _Q("In Kubernetes, the smallest deployable unit is a:",
           ["container", "pod (one or more containers sharing network/storage)", "node", "service"], 1,
           "Kubernetes schedules pods, not bare containers."),
        _Q("A Kubernetes Service provides:",
           ["persistent disk", "a stable virtual IP / DNS name load-balancing across pod replicas",
            "container images", "log storage"], 1,
           "Pods are ephemeral; a Service gives clients a stable endpoint to reach them."),
        _Q("Why prefer a small base image (e.g. alpine, distroless)?",
           ["It runs faster always", "Smaller attack surface and faster pulls/builds",
            "It allows more CPU", "It is required by Kubernetes"], 1,
           "Fewer packages means fewer CVEs and lighter images to move around."),
    ],
    "aws / cloud architecture": [
        _Q("The AWS shared-responsibility model means AWS is responsible for:",
           ["your IAM policies", "security *of* the cloud (hardware, facilities, managed service infra)",
            "your application code", "your S3 bucket permissions"], 1,
           "AWS secures the underlying infrastructure; the customer secures what they put in it."),
        _Q("To run stateless code without managing servers, you would use:",
           ["EC2 with cron", "AWS Lambda", "an on-prem VM", "a physical load balancer"], 1,
           "Lambda executes functions on demand with no server management."),
        _Q("An S3 bucket should almost never be:",
           ["versioned", "publicly writable to everyone", "encrypted", "in a region"], 1,
           "Public write access invites data tampering and abuse; keep buckets private by default."),
        _Q("Auto Scaling Groups improve resilience by:",
           ["reducing cost only", "replacing unhealthy instances and adjusting capacity to load",
            "encrypting traffic", "caching DNS"], 1,
           "ASGs maintain desired healthy capacity and scale with demand."),
    ],
    "digital logic & computer architecture": [
        _Q("How many distinct values can 8 bits represent?",
           ["128", "255", "256", "512"], 2,
           "2^8 = 256 values (0 to 255)."),
        _Q("A CPU cache exists primarily to:",
           ["store the OS", "hide main-memory latency by keeping recently/likely-used data close",
            "increase clock speed", "replace registers"], 1,
           "Caches exploit locality of reference to reduce average memory access time."),
        _Q("In two's complement, the most significant bit represents:",
           ["parity", "the sign (and a negative weight)", "an overflow flag", "the exponent"], 1,
           "The MSB has weight -2^(n-1), which encodes negative numbers."),
        _Q("Pipelining a processor improves:",
           ["single-instruction latency", "instruction throughput by overlapping stages",
            "cache size", "power draw"], 1,
           "Each stage works on a different instruction, so more instructions finish per unit time."),
    ],
    "verilog & systemverilog rtl": [
        _Q("In Verilog, which assignment models sequential (clocked) logic inside `always_ff`?",
           ["blocking `=`", "non-blocking `<=`", "`assign`", "`initial`"], 1,
           "Non-blocking assignments schedule updates together, matching flip-flop behaviour."),
        _Q("A `wire` in Verilog must be driven by:",
           ["a procedural block only", "a continuous assignment or module output",
            "nothing", "a testbench only"], 1,
           "Nets like `wire` need a continuous driver; `reg`/`logic` hold procedural assignments."),
        _Q("An incompletely specified combinational `always` block can infer:",
           ["a faster circuit", "an unintended latch", "a flip-flop", "a clock"], 1,
           "Missing else/default branches make a signal hold its value, inferring a latch."),
        _Q("Synthesis translates RTL into:",
           ["a waveform", "a gate-level netlist mapped to the target library",
            "C code", "a bitstream directly"], 1,
           "Synthesis produces a netlist; place-and-route then produces the physical/bitstream form."),
    ],
    "matlab & simulink model-based design": [
        _Q("In MATLAB, why prefer vectorised code over explicit loops?",
           ["it is required", "it runs faster using optimised array operations",
            "loops are illegal", "it uses less disk"], 1,
           "MATLAB array operations call optimised libraries, avoiding interpreter loop overhead."),
        _Q("A Simulink 'solver' is responsible for:",
           ["drawing blocks", "numerically integrating the model over time",
            "compiling C", "storing results"], 1,
           "The solver advances the simulation, choosing time steps for the dynamics."),
        _Q("Fixed-step solvers are typically required for:",
           ["research plots", "code generation targeting real-time hardware",
            "symbolic math", "plotting only"], 1,
           "Real-time targets need deterministic, uniform time steps."),
        _Q("Model-based design's main benefit is:",
           ["prettier diagrams", "simulating and verifying behaviour before writing/deploying code",
            "avoiding math", "faster typing"], 1,
           "You catch design errors in simulation, then generate code from the validated model."),
    ],
    "sql & data modeling": [
        _Q("Which JOIN returns only rows with a match in both tables?",
           ["LEFT JOIN", "INNER JOIN", "FULL OUTER JOIN", "CROSS JOIN"], 1,
           "INNER JOIN drops unmatched rows from both sides."),
        _Q("A primary key must be:",
           ["nullable", "unique and non-null", "a single column always", "auto-incrementing"], 1,
           "It uniquely identifies each row, so it cannot be null or duplicated."),
        _Q("Adding an index to a column speeds up reads but:",
           ["also speeds up every write", "slows down inserts/updates and uses extra storage",
            "removes the need for a WHERE clause", "changes the data type"], 1,
           "The index must be maintained on every write and occupies disk."),
        _Q("Normalisation to 3NF primarily aims to:",
           ["make queries longer", "remove redundant data and update anomalies",
            "denormalise for speed", "drop foreign keys"], 1,
           "Each non-key attribute depends on the key, the whole key, and nothing but the key."),
    ],
    "javascript & typescript": [
        _Q("What does `===` check that `==` does not?",
           ["value only", "value AND type, with no coercion", "reference only", "nothing different"], 1,
           "`===` is strict equality; `==` performs type coercion first."),
        _Q("`const arr = [1,2]; arr.push(3);` - is this allowed?",
           ["No, const forbids it", "Yes; const binds the reference, the array contents can still change",
            "Only in strict mode", "Only for numbers"], 1,
           "`const` prevents reassignment of the binding, not mutation of the object it points to."),
        _Q("TypeScript's main value over plain JavaScript is:",
           ["faster runtime", "static type checking that catches errors before running",
            "smaller bundles", "no need for a compiler"], 1,
           "Types are checked at compile time and then erased; runtime behaviour is unchanged."),
        _Q("`await` can only be used:",
           ["anywhere", "inside an `async` function (or top-level module in modern runtimes)",
            "inside a loop only", "in TypeScript only"], 1,
           "`await` pauses an async function until the promise settles."),
    ],
    "react & modern front-end": [
        _Q("Why must the `key` prop on list items be stable and unique?",
           ["for CSS", "so React can match elements across renders and avoid needless re-mounts",
            "it is optional styling", "to sort the list"], 1,
           "Keys let React's reconciler identify which items changed, were added, or removed."),
        _Q("A React component re-renders when:",
           ["the file is saved", "its state or props change (or its parent re-renders)",
            "the user scrolls", "never automatically"], 1,
           "State/prop changes trigger reconciliation and a possible DOM update."),
        _Q("The dependency array of `useEffect` controls:",
           ["the return value", "when the effect re-runs after a render",
            "the component name", "the render order"], 1,
           "The effect runs after renders where a listed dependency changed (or once if empty)."),
        _Q("Putting derived data in state instead of computing it during render often causes:",
           ["faster apps", "stale/duplicated state that can get out of sync",
            "smaller bundles", "no effect"], 1,
           "Derive during render when you can; only store what truly needs to persist."),
    ],
    "computer vision & opencv": [
        _Q("Converting an image to grayscale before edge detection is done to:",
           ["make it prettier", "reduce it to one intensity channel so gradients are well defined",
            "increase resolution", "add colour"], 1,
           "Most classic edge detectors operate on a single intensity channel."),
        _Q("A convolution kernel (filter) slid over an image computes:",
           ["a histogram", "a weighted sum of each local neighbourhood",
            "the image size", "the file format"], 1,
           "Each output pixel is the dot product of the kernel with the underlying patch."),
        _Q("In OpenCV, images are loaded by default in which channel order?",
           ["RGB", "BGR", "grayscale", "HSV"], 1,
           "`cv2.imread` returns BGR, which trips up code that assumes RGB."),
        _Q("Thresholding an image produces:",
           ["a blurred image", "a binary (black/white) mask", "a colour histogram", "a bigger image"], 1,
           "Pixels above the threshold become one value, the rest another - a segmentation mask."),
    ],
    "networking & network security": [
        _Q("Which layer does TCP operate at in the TCP/IP model?",
           ["link", "internet", "transport", "application"], 2,
           "TCP is the transport layer, providing reliable ordered byte streams over IP."),
        _Q("The main purpose of TLS is to provide:",
           ["faster routing", "encryption, integrity, and server authentication for a connection",
            "IP address assignment", "packet compression"], 1,
           "TLS wraps the transport connection so eavesdroppers cannot read or tamper with it."),
        _Q("A firewall default-deny policy means:",
           ["all traffic is allowed unless blocked", "all traffic is blocked unless explicitly allowed",
            "only outbound is blocked", "logging is disabled"], 1,
           "Default-deny is the secure baseline: you whitelist only what is needed."),
        _Q("DNS primarily:",
           ["encrypts traffic", "resolves human-readable names to IP addresses",
            "assigns MAC addresses", "routes packets"], 1,
           "DNS is the distributed name-to-address lookup system."),
    ],
    "power systems & load flow analysis": [
        _Q("A load-flow (power-flow) study computes:",
           ["fault currents", "steady-state bus voltages and line power flows",
            "insulation strength", "harmonic distortion only"], 1,
           "Load flow solves the nonlinear power balance equations for normal operating conditions."),
        _Q("At a PV (generator) bus, which quantities are specified?",
           ["P and Q", "P and voltage magnitude |V|", "|V| and angle", "Q and angle"], 1,
           "A PV bus fixes real power and voltage magnitude; Q and angle are solved."),
        _Q("The slack (swing) bus is needed to:",
           ["set the frequency", "balance total generation, load, and losses and set the angle reference",
            "measure current", "trip breakers"], 1,
           "Losses are unknown until the solution, so one bus absorbs the mismatch."),
        _Q("Per-unit normalisation is used because it:",
           ["hides errors", "makes quantities comparable across voltage levels and simplifies transformer math",
            "increases accuracy of the CPU", "is legally required"], 1,
           "Per-unit removes turns-ratio factors and keeps values near 1.0 across the network."),
    ],
    "mechanics of materials & structural analysis": [
        _Q("Normal stress in an axially loaded bar is:",
           ["force times area", "force divided by cross-sectional area",
            "force divided by length", "area divided by force"], 1,
           "sigma = P / A."),
        _Q("The slope of the linear-elastic region of a stress-strain curve is the:",
           ["yield strength", "Young's modulus (E)", "Poisson's ratio", "ultimate strength"], 1,
           "E = stress / strain in the elastic region (Hooke's law)."),
        _Q("A statically determinate structure is one where:",
           ["it never deflects", "equilibrium equations alone determine all reactions and internal forces",
            "it has redundant members", "it is always safe"], 1,
           "If unknowns exceed equilibrium equations, it is indeterminate and needs compatibility."),
        _Q("Bending stress in a beam is largest:",
           ["at the neutral axis", "at the outer fibres farthest from the neutral axis",
            "at the supports only", "uniformly across the section"], 1,
           "sigma = M*y/I, so stress grows linearly with distance y from the neutral axis."),
    ],
    "linear algebra & kinematics": [
        _Q("A homogeneous transformation matrix (4x4) in robotics combines:",
           ["only rotation", "rotation and translation in one operator",
            "only translation", "scaling only"], 1,
           "The 3x3 rotation block plus the translation column let you compose frames by multiplication."),
        _Q("Forward kinematics computes:",
           ["joint angles from end-effector pose", "end-effector pose from joint angles",
            "motor torques", "sensor noise"], 1,
           "Forward kinematics maps joint space to Cartesian pose; inverse kinematics is the reverse."),
        _Q("The Jacobian of a manipulator relates:",
           ["mass to force", "joint velocities to end-effector linear/angular velocity",
            "voltage to current", "position to time"], 1,
           "v = J(q) * q_dot; the Jacobian also reveals singularities where it loses rank."),
        _Q("A singular configuration of a robot arm means:",
           ["maximum stiffness", "the arm loses a degree of freedom of instantaneous motion",
            "the motors are off", "the workspace is largest"], 1,
           "At a singularity the Jacobian drops rank and some end-effector motions are impossible."),
    ],
    "digital electronics & circuit diagnostics": [
        _Q("In a pull-up resistor configuration, an unpressed push-button input reads:",
           ["logic 0", "logic 1", "floating", "high current"], 1,
           "The pull-up holds the node high until the button connects it to ground."),
        _Q("A decoupling (bypass) capacitor near an IC's power pin is there to:",
           ["increase voltage", "supply transient current and suppress supply noise",
            "divide the clock", "store data"], 1,
           "It provides a local charge reservoir so fast current spikes do not sag the rail."),
        _Q("Ohm's law relates:",
           ["power, time, energy", "voltage, current, and resistance (V = IR)",
            "frequency and capacitance", "charge and mass"], 1,
           "V = I * R is the defining relationship for a resistive element."),
        _Q("A logic probe showing a voltage halfway between VIL and VIH usually means:",
           ["the node is fine", "a floating node, contention, or a broken driver",
            "the supply is too high", "the clock stopped"], 1,
           "A valid logic node should sit near a rail; a mid-level reading signals a fault."),
    ],
    "data engineering & etl pipelines": [
        _Q("The difference between ETL and ELT is:",
           ["ELT is older", "ELT loads raw data first and transforms inside the warehouse",
            "they are identical", "ETL never transforms"], 1,
           "ELT defers transformation to the (scalable) warehouse compute."),
        _Q("Idempotency in a data pipeline job means:",
           ["it runs faster each time", "re-running it produces the same result without duplicates",
            "it cannot fail", "it needs no schedule"], 1,
           "Idempotent jobs are safe to retry after a failure - critical for reliability."),
        _Q("A schema-on-read system (e.g. a data lake) differs from schema-on-write because it:",
           ["validates structure at ingest", "stores raw data and applies structure at query time",
            "rejects JSON", "cannot store CSV"], 1,
           "Flexibility at ingest, but consumers must handle messy/variable data."),
        _Q("Partitioning a large table by date primarily helps by:",
           ["reducing storage cost to zero", "letting queries scan only the relevant partitions",
            "removing the need for indexes", "encrypting the data"], 1,
           "Partition pruning cuts the amount of data a time-bounded query must read."),
    ],
    "fpga synthesis (xilinx / altera)": [
        _Q("An FPGA implements logic using:",
           ["fixed ASIC gates", "configurable look-up tables (LUTs), flip-flops and routing",
            "a fixed CPU core only", "software emulation"], 1,
           "LUTs are programmed to realise arbitrary combinational functions, wired by the fabric."),
        _Q("Timing closure fails when:",
           ["the design is too small", "a path's delay exceeds the clock period (setup violation)",
            "there are unused LUTs", "the bitstream is large"], 1,
           "If combinational delay between registers is too long, data misses the capturing edge."),
        _Q("Pipelining a long combinational path in RTL improves:",
           ["area only", "the maximum clock frequency by shortening per-stage delay",
            "power to zero", "the reset behaviour"], 1,
           "Inserting registers splits the critical path, raising Fmax at the cost of latency."),
        _Q("A constraints (XDC/SDC) file mainly specifies:",
           ["test vectors", "clock periods and pin assignments for the tools",
            "the C testbench", "simulation waveforms"], 1,
           "Without clock/IO constraints the tools cannot verify or meet timing."),
    ],
    "ev powertrain & battery management": [
        _Q("A Battery Management System (BMS) primarily protects cells by:",
           ["increasing capacity", "monitoring voltage/current/temperature and balancing cells",
            "cooling the cabin", "charging faster than rated"], 1,
           "The BMS keeps every cell inside its safe operating window and equalises state of charge."),
        _Q("State of Charge (SoC) is:",
           ["the pack voltage", "the remaining usable capacity as a percentage of full",
            "the C-rate", "the internal resistance"], 1,
           "SoC is analogous to a fuel gauge for the battery pack."),
        _Q("Regenerative braking recovers energy by:",
           ["using friction pads", "running the motor as a generator to charge the battery",
            "venting heat", "downshifting"], 1,
           "The traction motor converts kinetic energy back to electrical energy during deceleration."),
        _Q("Thermal runaway in a lithium cell is dangerous because:",
           ["it is slow", "self-heating accelerates exothermic reactions, propagating to neighbours",
            "it only lowers voltage", "it improves life"], 1,
           "Once triggered, the reaction is self-sustaining and can cascade through the pack."),
    ],
    "solar pv & wind farm engineering": [
        _Q("A solar panel's Maximum Power Point Tracker (MPPT) exists to:",
           ["clean the panels", "continuously operate the array at its peak power voltage as conditions change",
            "store energy", "invert AC to DC"], 1,
           "Irradiance and temperature shift the I-V curve; MPPT keeps the operating point optimal."),
        _Q("Partial shading of one cell in a series string causes:",
           ["higher output", "a large drop in string output (and hot-spotting) unless bypass diodes act",
            "no effect", "a frequency shift"], 1,
           "The weakest cell limits series current; bypass diodes route around shaded cells."),
        _Q("Wind turbine power scales approximately with:",
           ["wind speed", "the cube of wind speed", "the square root of wind speed", "blade colour"], 1,
           "P is proportional to v^3, so small speed increases yield large power gains."),
        _Q("The capacity factor of a renewable plant is:",
           ["its nameplate rating", "actual energy produced divided by energy if it ran at full rating",
            "its efficiency", "the grid frequency"], 1,
           "It captures how much of the theoretical maximum output was actually delivered."),
    ],
    "bim & autodesk revit structure": [
        _Q("BIM (Building Information Modeling) differs from plain CAD because the model:",
           ["is 2D only", "carries parametric objects with data (materials, cost, schedule)",
            "cannot be printed", "has no geometry"], 1,
           "BIM elements are intelligent objects, enabling clash detection, quantities and coordination."),
        _Q("A 'clash detection' run finds:",
           ["spelling errors", "physical conflicts between disciplines (e.g. a duct through a beam)",
            "missing fonts", "over-budget items"], 1,
           "Coordinating models exposes interferences before they reach the site."),
        _Q("A Revit 'family' is:",
           ["a project file", "a reusable parametric component definition (door, column, etc.)",
            "a user group", "a rendering setting"], 1,
           "Families define geometry and parameters; instances are placed in the project."),
        _Q("Level of Development (LOD) describes:",
           ["the building height", "how detailed and reliable a model element is at a project stage",
            "the file size", "the number of users"], 1,
           "LOD 100-500 communicates whether an element is conceptual or construction-ready."),
    ],
    "arm cortex architecture & registers": [
        _Q("The ARM Cortex-M stack pointer after reset is loaded from:",
           ["address 0x0", "the first word of the vector table", "a GPIO register", "the linker script at runtime"], 1,
           "The first vector-table entry is the initial MSP; the second is the reset handler address."),
        _Q("Cortex-M cores are:",
           ["CISC with microcode", "load/store RISC architectures", "stack machines", "8-bit"], 1,
           "ARM is a load/store RISC ISA - ALU ops work on registers, not memory directly."),
        _Q("The NVIC in a Cortex-M is responsible for:",
           ["floating point", "prioritised interrupt handling and vectoring", "DMA transfers", "clock generation"], 1,
           "The Nested Vectored Interrupt Controller manages IRQ priorities and tail-chaining."),
        _Q("A 'HardFault' on Cortex-M is commonly caused by:",
           ["a compiler warning", "an invalid memory access or an unaligned/illegal instruction", "low battery", "too many comments"], 1,
           "Bad pointers, stack overflow, or bus errors escalate to HardFault."),
    ],
    "node.js & backend services": [
        _Q("Node.js handles many concurrent connections efficiently because it:",
           ["spawns a thread per request", "uses a single-threaded non-blocking event loop with async I/O",
            "runs on the GPU", "blocks on every call"], 1,
           "I/O is offloaded and callbacks run on the event loop, avoiding per-connection threads."),
        _Q("Blocking the event loop with a long synchronous computation causes:",
           ["faster responses", "all other requests to stall until it finishes",
            "a memory leak", "a new process"], 1,
           "Nothing else runs while the loop is busy - offload CPU-heavy work to workers."),
        _Q("Middleware in an Express app is:",
           ["a database", "a function with access to req/res that runs in the request pipeline",
            "a template engine", "a port number"], 1,
           "Middleware can inspect, modify, end, or pass the request to the next handler."),
        _Q("Storing secrets (DB passwords, API keys) should be done via:",
           ["hard-coded constants", "environment variables / a secrets manager, never in source",
            "a public README", "client-side JS"], 1,
           "Secrets in source control leak; inject them from the environment at runtime."),
    ],
    "applied cryptography": [
        _Q("Symmetric encryption (e.g. AES) differs from asymmetric (e.g. RSA) in that symmetric:",
           ["uses two keys", "uses one shared secret key for encrypt and decrypt",
            "cannot be broken", "is only for signatures"], 1,
           "Asymmetric uses a public/private key pair; symmetric shares one key."),
        _Q("A cryptographic hash function must be:",
           ["reversible", "collision-resistant and one-way", "encrypted", "shorter than the input always by half"], 1,
           "You must not be able to invert it or easily find two inputs with the same digest."),
        _Q("Passwords should be stored as:",
           ["plaintext", "a salted slow hash (bcrypt/scrypt/argon2)", "base64", "MD5"], 1,
           "Salting defeats rainbow tables; a slow KDF defeats brute force."),
        _Q("The purpose of a nonce / IV in a cipher mode is to:",
           ["speed it up", "ensure identical plaintexts encrypt to different ciphertexts",
            "shorten the key", "authenticate the user"], 1,
           "Reusing an IV (especially in CTR/GCM) can catastrophically break confidentiality."),
    ],
    "iot connectivity & protocols (mqtt, ble, lora)": [
        _Q("MQTT's publish/subscribe model routes messages via:",
           ["direct device-to-device sockets", "a broker and hierarchical topics",
            "email", "a blockchain"], 1,
           "Clients publish to topics on a broker; other clients subscribe to those topics."),
        _Q("LoRa is chosen for IoT when you need:",
           ["gigabit throughput", "very long range at low power and low data rate",
            "video streaming", "sub-millisecond latency"], 1,
           "LoRa trades bandwidth for kilometre-scale range on a coin cell."),
        _Q("BLE (Bluetooth Low Energy) is optimised for:",
           ["continuous high-bandwidth audio", "short bursts of data with long sleep periods",
            "wired links", "mains-powered devices only"], 1,
           "BLE keeps the radio off most of the time, waking briefly to exchange small packets."),
        _Q("MQTT QoS level 2 guarantees:",
           ["at most once", "exactly once delivery via a four-part handshake",
            "no delivery", "fastest delivery"], 1,
           "QoS 2 is the most reliable and most expensive; QoS 0 is fire-and-forget."),
    ],
    "ui automation (selenium/cypress/playwright)": [
        _Q("A 'flaky' UI test is one that:",
           ["always fails", "passes or fails non-deterministically without code changes",
            "runs too fast", "has no assertions"], 1,
           "Flakiness usually comes from timing/async races or shared state - fix with proper waits."),
        _Q("The most robust way to locate an element for a test is:",
           ["its pixel position", "a stable attribute like data-testid or role/label",
            "a long brittle XPath", "its text colour"], 1,
           "Semantic or dedicated test attributes survive refactors that break CSS/XPath selectors."),
        _Q("Explicit waits (wait-for-condition) are preferred over fixed sleeps because they:",
           ["are longer", "return as soon as the condition is met, reducing time and flakiness",
            "disable the browser", "skip assertions"], 1,
           "Fixed sleeps are either too short (flaky) or too long (slow); condition waits adapt."),
        _Q("The testing pyramid suggests you should have:",
           ["mostly end-to-end UI tests", "many fast unit tests, fewer integration, fewest E2E",
            "only manual tests", "equal numbers of each"], 1,
           "UI/E2E tests are slow and brittle - keep them thin over a broad unit-test base."),
    ],
    "3d cad modeling (solidworks/fusion360)": [
        _Q("A fully defined sketch in parametric CAD is one where:",
           ["it has colour", "every entity is fixed by dimensions and constraints (no free movement)",
            "it is 3D", "it has no dimensions"], 1,
           "Under-defined sketches move unpredictably when parameters change."),
        _Q("Design intent in feature-based modeling means:",
           ["the render style", "structuring features/relations so edits propagate the way you intend",
            "the file format", "the material colour"], 1,
           "Good design intent makes a model robust to dimension changes."),
        _Q("A fillet feature is typically added:",
           ["first, before any geometry", "late in the feature tree, after the main shape",
            "only in 2D", "never in CAD"], 1,
           "Adding fillets/chamfers last keeps earlier features simple and edits stable."),
        _Q("GD&T (geometric dimensioning and tolerancing) communicates:",
           ["only nominal sizes", "allowable variation in form, orientation, and location for manufacturing",
            "the assembly order", "the render resolution"], 1,
           "GD&T tells the machinist and inspector how much geometric deviation is acceptable."),
    ],
    "finite element analysis (fea)": [
        _Q("FEA approximates a continuous domain by:",
           ["a single equation", "dividing it into many small elements with nodal interpolation",
            "measuring it physically", "ignoring boundaries"], 1,
           "The PDE is solved approximately over a mesh of simple elements."),
        _Q("Refining the mesh in a region of high stress gradient generally:",
           ["reduces accuracy", "improves accuracy at higher computational cost",
            "has no effect", "removes the stress"], 1,
           "Convergence studies increase mesh density until results stop changing meaningfully."),
        _Q("An unconstrained FEA model (missing boundary conditions) will typically:",
           ["solve fine", "fail to solve or show rigid-body motion / singular stiffness matrix",
            "run faster", "give the exact answer"], 1,
           "You must restrain rigid-body modes for a static solution to exist."),
        _Q("Stress singularities at sharp re-entrant corners mean:",
           ["the part is perfect there", "stress grows without bound as the mesh refines - use a fillet or interpret with care",
            "the mesh is too coarse only", "the material failed"], 1,
           "Idealised sharp corners are non-physical; real parts have radii."),
    ],
    "aerodynamics & fluid mechanics": [
        _Q("The Reynolds number is the ratio of:",
           ["lift to drag", "inertial forces to viscous forces", "pressure to temperature", "mass to volume"], 1,
           "Low Re is laminar-dominated; high Re is turbulence-dominated."),
        _Q("Bernoulli's principle (incompressible, inviscid) states that where flow speed increases:",
           ["static pressure increases", "static pressure decreases", "density increases", "temperature is constant only"], 1,
           "Along a streamline, dynamic and static pressure trade off."),
        _Q("Induced drag on a finite wing is caused by:",
           ["skin friction", "wingtip vortices / downwash from the trailing vortex system",
            "shock waves", "surface roughness"], 1,
           "Higher aspect ratio wings reduce induced drag."),
        _Q("Flow separation (stall) occurs when:",
           ["the boundary layer stays attached", "an adverse pressure gradient causes the boundary layer to detach",
            "speed is too low only", "the air is cold"], 1,
           "Beyond the critical angle of attack the flow can no longer follow the surface."),
    ],
}
GENERIC = [
    _Q("When a course sits later in a prerequisite-ordered roadmap, it usually means:",
       ["it is optional", "it depends on skills the earlier courses build",
        "it is easier", "it can be skipped"], 1,
       "Later phases assume the vocabulary and tools taught earlier."),
    _Q("The most reliable evidence that you have actually learned a course's material is:",
       ["watching every video at 2x", "shipping a small project that applies it end to end",
        "bookmarking it", "reading the syllabus"], 1,
       "Applied output demonstrates transfer; passive consumption does not."),
    _Q("If a course feels too hard, the best move is usually to:",
       ["push through regardless", "revisit its prerequisite course, then return",
        "skip to the capstone", "abandon the roadmap"], 1,
       "A difficulty spike normally signals a gap in an earlier, foundational skill."),
]